import { CheckoutForm } from "@/components/CheckoutForm";
import { Section } from "@/components/Section";

export default function Checkout() {
  return (
    <Section icon="💳" title="Checkout">
      <CheckoutForm />
    </Section>
  );
}
